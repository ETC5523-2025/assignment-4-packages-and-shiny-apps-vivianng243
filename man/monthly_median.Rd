% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/median_summary.R
\name{monthly_median}
\alias{monthly_median}
\title{Monthly median for a selected site and month}
\usage{
monthly_median(data, site, month, value = surfWaterNitrateMean)
}
\arguments{
\item{data}{A tibble or data frame (e.g. \code{nitrate_tidy} or \code{temp_tidy}).}

\item{site}{Site code to filter (e.g. \code{"LEWI"}, \code{"ARIK"}, \code{"CARI"}).}

\item{month}{Month label to filter (e.g. \code{"Jan"}, \code{"Feb"}, ... \code{"Dec"}).}

\item{value}{Name of numeric column to summarise (default = \code{surfWaterNitrateMean}).}
}
\value{
A single numeric value (the median) or \code{NA} if no matching data.
}
\description{
Returns the median of the numeric column in a dataset like \code{nitrate_tidy}
or \code{temp_tidy} for a specific site and month.
}
\examples{
# Median nitrate for LEWI in January
monthly_median(nitrate_tidy, "LEWI", "Jan", surfWaterNitrateMean)

# Median temperature for LEWI in January
monthly_median(temp_tidy, "LEWI", "Jan", surfWaterTempMean)
}
